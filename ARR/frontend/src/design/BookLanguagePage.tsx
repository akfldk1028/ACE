import { BookLanguageFlow } from './components/book-language-flow/BookLanguageFlow';

export default function BookLanguagePage() {
  return (
    <main className="book-language-page">
      <BookLanguageFlow standalone />
    </main>
  );
}
