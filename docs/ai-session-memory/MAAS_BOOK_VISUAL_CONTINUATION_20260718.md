# MAAS BOOK visual continuation checkpoint — 2026-07-18

Read this before continuing the neighborhood BOOK portfolio. Direct PNG review
remains the authority. None of the runs below is visually accepted.

## Real-PNU evidence

PNU: `1168011800104170004`

- r111 (`v20` typed arity): 14/20 and the strongest count in this continuation,
  but direct PNG review still found a compact box/bar majority with shallow
  notches and weak primary volumetric relations.
- r112 (r111 portfolio feedback): regressed to 10/20. The author copied generic
  hall semantics into neighborhood programs; this run is not a new best.
- r113 (`v22` program-role access binding): 9/20. PNG:
  `docs/playwright/design-route-live-verify/book-program-portfolios-r113-v22-program-role-access-live-vlm/maas-book-neighborhood-20.png`.
  All nine are still small single-box derivatives; the final portfolio VLM also
  reported too few candidates, repeated footprint/roof and fragmented Lego.
- r114 (`v24` longitudinal/non-fragmenting author): 11/20. PNG:
  `docs/playwright/design-route-live-verify/book-program-portfolios-r114-v24-longitudinal-nonfragmenting-live-vlm/maas-book-neighborhood-20.png`.
  Cards 1/3/4/9/11 are somewhat larger coherent bodies, but still box-derived.
  Cards 5/6/7/8/10 accumulate lift/stack/branch/expand effects and visibly read
  as toy fragments. This is a direct visual failure, not an improvement claim.

## Causes proved in code and PNG

1. The LLM author used current invariant roles (`void_or_terrace`,
   `street_access`) that the old access-relation classifier did not recognize.
2. Generic prompt examples encouraged fake terminal hall/gallery roles in a
   neighborhood program.
3. `cross_mass`, `split_wing` and `bent_bar` could start from a square BLOCK.
   Cross then became identity-like and split became two short toy chunks.
4. `cross_mass + access-bound open courtyard/carve_void` cut an already
   repeated plan into four separated fragments. A targeted PNG probe visibly
   proved this failure.
5. The source bridge reused the small legacy component union as a second area
   cap even though the height-safe legal host was already authoritative.
6. After the source was improved, BOOK projection reintroduced accumulation.
   BOOK `lift` discarded the program access side and therefore emitted four
   corner supports; multi-call `stack/branch/pack` sentences could add several
   new visual rules over an already articulated recursive source.

## Code state after r114

- Geometry author prompt contract is
  `arr.maas.geometry_llm_author.v24_nonfragmenting_relation_pairs`.
- Program-specific semantic roles replace generic hall examples, fake terminal
  rename nodes are forbidden, and typed node arity is operator-specific.
- Relation-role recognition includes void/terrace/access tokens.
- Cross/split/bent reject BLOCK in the LLM validator and deterministic synthesis
  prefers BAR when available.
- Repeated plan relations followed by an access-bound open court are rejected;
  cross should use an access-bound lift or shallow side notch, while split uses
  its own access relation.
- Neighborhood recursive materialization requests at least 0.58 of the legal
  host as its target plan-area cap. Longitudinal aspect preservation can still
  keep the realized footprint below that target; it is not a fake legal pass.
- Targeted local PNGs in
  `docs/playwright/design-route-live-verify/r114-targeted-scale-probe/` prove:
  BAR bent+notch is one long clean body; BAR split+bridge is one long connected
  U/bridge body; cross+open-court fragments and is now forbidden.
- Post-r114, BOOK projection now inherits the program cardinal access side for
  `lift` and `split_wing`. Lift therefore uses one access-bound service spine,
  and split uses bridge+ground spine instead of detached pieces.
- Post-r114, the neighborhood program form gate limits source body rules plus
  BOOK body rules to two. The separate protected public-threshold rule is not
  counted. This rejects source+stack+branch effect accumulation without
  weakening legal, parking, compiler, clean-mass or VLM gates.

The last two BOOK changes were made after r114 completed and are covered by
tests, but have not yet been verified in a new live full-loop PNG. Do not claim
them as visual success until r115 (or later) is rendered and inspected.

## Verification

`python ARR\\backend\\manage.py test design.test_maas_geometry_language --verbosity 1`

- 122 tests
- 122 passed
- runtime about 152 seconds

## Next bounded step

Run one new live neighborhood portfolio from the workspace root so the author
cache stays under the repository `docs` tree. Inspect
`maas-book-neighborhood-20.png` at original resolution before reading the
numeric count. The required visual facts are:

- long coherent bent/split bodies survive BOOK projection;
- no four-column lift, detached cube pair or star-like cross/open-court;
- no multi-effect stack/branch accumulation over an articulated source;
- a true through-court, one access-bound lifted spine and one legible bent/split
  relation are present as different primary volumetric systems;
- legal and parking hard gates remain unchanged.

If the resulting board still converges to compact boxes, do not relax the
visual gate or pad to 20. Inspect which single BOOK operations remain visually
weak and repair their executable mapping rather than adding quotas.

## r119 page-faithful corpus + early typology checkpoint

The original 69 scans under `docs/260506/BOOK` were visually inspected one by
one at original resolution before this change. The executable source contract
is now split from registry/search code.

- p.3: six exact base-volume relations: `1/1`, `3/8`, `1/2`, `1/4`, `1/8`,
  `1/16`.
- pp.6–37: 30 operatives, each retaining the shown three-step procedure,
  three orientations and eleven-variation row.
- pp.39–48: 20 sequential combinations.
- pp.50–58: nine aggregation recipes, with diagram execution order distinct
  from heading display order where required.
- pp.60–69: ten case studies are now executable fourth-stage principles. Their
  program/entry/core/plinth/roof implementation elements remain attached to
  candidate lineage instead of being `evidence_only` labels.
- The registry/audit therefore covers 69 executable principles: 30 + 20 + 9 +
  10.

The old frontend language was also restored as a separate early chassis layer,
not confused with p.3 scope or the five normalized primitive seeds:
`additive`, `subtractive`, `grid`, `lshape`, `ushape`, `cross`, `courtyard`,
`tower_podium`, `hshape`, `radial`, `freeform`. `grid_mass` is a connected
row/column field rather than another alias for `cross_mass`.

Important hard boundaries:

- actual SourceMass promotion now requires exactly one connected recursive
  solid; multi-component compiler probes may not enter the program/legal/VLM
  pool;
- repeated nonlinear BOOK deformation is rejected over an existing
  cross/grid/radial/split field;
- representative BOOK states are the full eleven-state lattice, with bounded
  parameters decorrelated instead of all moving by the same fraction;
- the three representative states are `0/5/10` and visit all three
  orientations; recursive one-probe scope rotates independently so all six
  p.3 volumes remain reachable;
- deterministic program-profile supply is 18 chassis candidates per lineage,
  not 12, because strict diversity caps must be satisfied through generation
  rather than padding or relaxing selection.

Non-live-VLM real-PNU run r119:

- output:
  `docs/playwright/design-route-live-verify/book-program-portfolios-r119-six-scope-expanded-chassis/`
- status: pass, 20/20; direct PNG inspected at original resolution;
- all six p.3 scopes represented;
- 19 distinct BOOK principles; selected mix includes 10 base, 6 combination,
  1 aggregation and 3 case-study candidates (the summary count omission for
  case studies was subsequently fixed);
- 18 seed families, 7 chassis families, 13 recursive geometry families, 15
  measured visual languages;
- 432 evaluated, 317 compiled, 309 clean, 178 program-hard-pass;
- all selected recursive solids have component count 1;
- legal, geometry retention and parking: 20/20; minimum legal volume retention
  0.8055;
- 157 combined BOOK/geometry regression tests pass.

This is not a live-VLM acceptance claim. The current shell had no
`OPENAI_API_KEY`, `MAAS_LIVE_GEOMETRY_VLM`, or
`MAAS_LIVE_VLM_CREDENTIAL_ROTATED`. Direct review still flags some pyramidal
or overly articulated candidates (notably the taper lane) for a future exact
post-BOOK VLM rejection/repair pass. Do not weaken those gates or describe
r119 as competition-final until that live image-grounded pass is run.

## r121 base-volume host system + activation-flow checkpoint (2026-07-19)

The code was reorganized around explicit semantic axes instead of adding more
branches to the benchmark monolith. `design/maas/language_system.py` is a thin
read-only manifest over the existing source modules and preserves this order:

1. source evidence (69 inspected BOOK pages);
2. p.3 relative base volume (`1/1`, `3/8`, `1/2`, `1/4`, `1/8`, `1/16`);
3. orientation (`horizontal`, `vertical`, `diagonal`);
4. base host seed (`block`, `slab`, `bar`, `tower`, `profiled_prism`);
5. eleven early frontend chassis;
6. 69 BOOK principles;
7. eleven representative variations;
8. three programs;
9. compatibility, geometry, program, legal, parking and visual hard gates;
10. typed live-VLM critique/repair;
11. selected portfolio and outcome memory.

This yields 90 exact p.3 host states and 2,254,230 theoretical program paths
before compatibility and hard gates. No path is padded into a selected
portfolio. Base volume is host-relative: a `1/1` SLAB is deliberately a wide
plate/full podium, a `1/2` SLAB a half plate/podium band, a `1/1` TOWER a full
tower body and a `1/4` BAR a local linear wing segment. A new cross-host audit
compiled all 90 states as connected hard-pass geometry.

Evidence:

- `docs/playwright/design-route-live-verify/r120-book-from-first-principles/book-p3-base-volume-host-90.json`
- `docs/playwright/design-route-live-verify/r120-book-from-first-principles/book-p3-base-volume-host-90.png`
- `GET /design/maas/language-system/`
- `docs/playwright/design-route-live-verify/r121-language-system-frontend/design-language-flow-fullscreen-vlm-portal.png`

The frontend now renders the full hierarchy as a dark, horizontally scrollable
activation-flow graph inspired by the two KakaoTalk reference PNGs. It shows
many executable path edges plus the VLM revision feedback loop, but explicitly
does not claim to expose model-internal neurons. Fullscreen uses a body portal,
so the header and controls no longer disappear under the route panel's
transformed/scrolling ancestor. The older agent graph now uses fit-to-content
instead of a hardcoded viewport.

Verification:

- backend focused suites: 178/178 passed;
- frontend `npm run type-check`: passed;
- frontend production build: passed (existing large-chunk warning only);
- browser `/design`: network idle, zero pending resources, no app error overlay;
- endpoint manifest: 69 pages, 6 scopes, 5 seeds, 11 chassis, 69 principles,
  2,254,230 pre-gate paths.

### r121 live VLM status while the loop is still running

The three-program command is running with live geometry VLM enabled. The first
completed board, neighborhood, selected 8/20 without padding. Direct original-
resolution review is **fail**: toy fragmentation is reduced; one wide
plate/podium-like candidate and several longer coherent bodies survive, but
the majority remains a close family of compact boxes, shallow grooves and
courtyard relatives. This is not architecture/competition grade. Gym and
cultural must be appended here only after their boards and final combined
summary are rendered and inspected.

## r122 universal form before program/VLM (2026-07-19)

A direct audit of `_program_pool()` found a structural mismatch behind the
r121 diagram: `_agent_mutated_seeds(building_type, ...)` authored the recursive
dominant solid from a program profile before p.3 scope and BOOK projection.
Although the exact final critic was post-BOOK, the initial body was already
gym-, housing- or cultural-conditioned. The actual execution order, not just
the diagram, has now been corrected.

New default order:

1. generate a shared 64-program universal form bank from five normalized base
   seeds and all eleven early chassis;
2. apply p.3 base-volume scope/orientation and BOOK language;
3. project program roles, access, section and capacity evidence downstream;
4. apply geometry/program/legal/retention/parking hard gates;
5. run image-grounded VLM critique and typed repair over the resulting
   program-aware BOOK geometry;
6. select without padding and write outcome memory.

The 64 universal programs all compile with one connected component. Their
serialized dominant-form payload set is identical for neighborhood, gymnasium
and cultural before program projection. Default `program_profile_control` and
`openai_vlm_experimental` dominant-form lanes were removed; explicit feedback
requests are additive only. The API is now `arr.maas.language_system.v2`, and
the activation graph shows Base seed -> Chassis -> Base volume -> BOOK ->
Program projection -> Hard gates -> Post-program VLM. Its feedback edge returns
to Program projection, never to the universal base/BOOK layers.

Evidence:

- `ARR/backend/design/maas/geometry_language/universal_form_bank.py`
- `docs/playwright/design-route-live-verify/r121-language-system-frontend/design-language-flow-universal-first-fullscreen.png`
- `docs/playwright/design-route-live-verify/r121-language-system-frontend/design-language-flow-universal-first-vlm.png`

The previous-order live run also found and reproduced a GEOS overlay crash in
the silhouette novelty metric. A shared safe symmetric-difference boundary now
repairs derived silhouettes and treats an irreparable comparison as duplicate
rather than rewarding invalid novelty. The resumed real run crossed the exact
former crash point. Verification: 180 focused backend tests, invalid bow-tie
and hole-outside-shell regressions, frontend type-check, production build,
manifest v2 API probe and real-browser DOM/screenshot checks all pass.

Old-order baseline visual verdict so far: neighborhood 8/20 fail; gym 8/20
fail. The gym board has program recognizability but repeats one low long-span
hall family with shallow ridge/groove/notch changes. Do not attribute either
board to the new universal-first code; the running Python process imported the
old pipeline before r122 was edited. Cultural and the combined baseline remain
pending.

### Old-order baseline completed

The resumed r121 process exited normally with zero stderr bytes and wrote the
three boards, combined board, 10 MB summary, exact geometry archive and 257 MB
outcome graph. Summary status is `fail`; numeric status is `fail`; downstream
hard-gate status is `pass` for all 21 selected sources.

- neighborhood: 8/20, eight BOOK operations, five visual languages, five p.3
  scopes, final geometry-VLM supply 11, portfolio-VLM fail;
- gymnasium: 8/20, eight BOOK operations, five visual languages, four p.3
  scopes, final geometry-VLM supply 15, portfolio-VLM fail;
- cultural: 5/20, four BOOK operations, five visual languages, five p.3 scopes,
  final geometry-VLM supply 6, portfolio-VLM fail.

All three portfolio critics report too few candidates, family resemblance,
repeated footprint and repeated roof. Neighborhood additionally reports box
dominance; gym and cultural report weak program language. The direct combined
board agrees. Cross-program minimum silhouette distances are 0.1128
(neighborhood/gym), 0.2125 (neighborhood/cultural) and 0.2039
(gym/cultural). Fingerprint:
`61d85d383dde81681391c60611943ee13e05f4243609894269d3f17b9eb1e782`.

Artifacts:

- `docs/playwright/design-route-live-verify/book-program-portfolios-r121-language-system-live-vlm/maas-book-programs-60-summary.png`
- `docs/playwright/design-route-live-verify/book-program-portfolios-r121-language-system-live-vlm/maas-book-programs-summary.json`
- `docs/playwright/design-route-live-verify/book-program-portfolios-r121-language-system-live-vlm/maas-book-exact-geometry-artifacts.json`

This closes the old execution-order baseline only. The next acceptance run must
start a fresh Python process that imports r122 universal-first supply and must
again be judged from its final PNGs.

## r124 post-BOOK program-relation projection

The first universal-first real-PNU preflight (r122) exposed a second structural
gap. The common form bank and BOOK projection were real, but the program layer
was still mostly wrapper/context metadata. The selected neighborhood board had
20 candidates and broad base/BOOK diversity, yet none of the final ASTs had a
frontage-aligned public-threshold controller and ten required design concepts
were unresolved.

The program layer is now a separate executable module:

- input: the already BOOK-projected `GeometryProgram`;
- contract source: `program_reference_contract(building_type)`;
- output: one access-bound public threshold and, only when required by the
  contract, a terminal program section controller;
- authority boundary: every added node records `program_conditioning_stage =
  after_book` and `base_or_book_authority = false`;
- VLM remains a downstream critic/typed repair authority only.

This does not select a completed form by use. Neighborhood, gym and cultural
still receive the exact same 64 pre-program payloads. The final BOOK topology
and program relation vocabulary choose among a compatible open court, shallow
entry notch or access-bound lift. Complex/distributed bodies do not receive an
open court that could fragment them, an existing lift is not stacked with a
second lift, and idempotent connectivity guards preserve one occupiable solid.
Gym alone receives the contract-required `profiled_hall` section after its
threshold; its base plan/body remains from the shared bank and BOOK.

Regression evidence:

- 64 universal forms x 3 programs = 192 projected programs, compiled 192/192
  with component count one;
- exact pre-program payload identity across all three uses remains tested;
- public threshold and gym section concept-graph controllers are present and
  access-aligned;
- full `design.test_maas_geometry_language`: 153/153 pass;
- `design.test_maas_book_scope`: 6/6 pass;
- benchmark stderr: zero bytes.

The first post-BOOK projection run r123 fixed the old failures exactly:
frontage-aligned threshold count 0 -> 20 and missing required concept count 10
-> 0. It still failed because a conservative first version used the same notch
for every candidate, reducing ground strategies to one. That result was not
accepted.

The corrected r124 neighborhood run passes all numeric and downstream gates:

- selected 20/20 without padding;
- 20 distinct BOOK operations, 17 measured visual languages;
- all six p.3 scopes: 1/1, 3/8, 1/2, 1/4, 1/8, 1/16;
- 20/20 frontage-aligned thresholds, zero missing design concepts;
- three ground strategies: 11 entry notches, eight lifted thresholds, one open
  court;
- six solid phenotypes, dominant phenotype share 0.25;
- downstream legal/geometry-retention/parking 20/20;
- overall/numeric/downstream statuses: pass/pass/pass; failures: none.

Artifact:
`docs/playwright/design-route-live-verify/book-program-portfolios-r124-post-book-program-diversity-neighborhood/maas-book-neighborhood-20.png`.

Direct original-resolution review is improved but remains provisional. The
board reads as one-building court, lifted, stepped, curved and split systems
rather than detached toy fragments. Candidates 15 and 17 remain too thin or
small, and candidate 19 remains overly skewed. A fresh r125 live-VLM run is
active over the new execution order. Its final image and audit, not r124's
numeric pass, decide visual acceptance.

## r125 first live VLM over universal -> BOOK -> program

r125 completed normally with zero stderr. It audited 68 exact candidate
renders under the neighborhood program contract and retained only seven. This
is a real visual filter, not a numeric proxy: the overly thin, small and skewed
r124 candidates were removed. All seven retained sources still pass downstream
legal/retention/parking gates.

The result is nevertheless **fail**:

- selected 7/20, seven BOOK operations, six visual languages;
- four p.3 scopes and two ground strategies;
- portfolio VLM failures: too few candidates, family resemblance, repeated
  footprint, repeated roof and weak program language;
- the portfolio critic identifies a repeated compact block/bar with one
  subtraction or threshold shift, plus a repeated lifted/calm block subgroup;
- direct PNG review agrees: candidates 1 and 3 are too generic, candidate 4 is
  over-articulated, and the seven do not form a competition-grade portfolio.

The exact typed-repair trace found a concrete implementation failure. The VLM
requested one repair: replace BOOK `taper` with `stepped_mass`, then set
direction, levels, setback and podium parameters. This is a valid unary-solid
replacement, but `mutation.py` retained the old `modifier` kind and produced
`modifier:stepped_mass`; therefore requested/revised/changed were 1/0/0.

`replace_operator` now resolves the operator's executable kind from
`OPERATORS_BY_KIND`, permits the safe unary modifier -> macro transition,
clears stale operator parameters, and still runs full AST arity validation and
the real compiler before accepting the edit. A regression reproduces the live
record and proves a connected geometry hash change. Full geometry-language
tests pass 154/154.

r125 also showed a review-bandwidth bottleneck: 286 viable base-stage candidates
fed a 64-image shortlist, of which only 11 passed. r126 expands only the review
budget to the existing supported maximum of 96; visual thresholds, similarity,
program, legal and parking gates remain unchanged. r126 is active and must be
judged from its final PNG rather than assumed to improve.

## r126 live repair proof, still not enough families

r126 completed with zero stderr. The 96-image review budget was exercised on
96 of 286 base-stage candidates, reusing 64 cached audits and calling only the
32 additional reviews. Nineteen base parents passed, versus eleven at budget
64. The final portfolio increased from seven to ten candidates.

The cross-kind mutation fix is proven in the real loop:

- typed repairs requested/revised/geometry-changed: 3/3/3;
- source materialized / clean+program passed: 2/2;
- mutation issues and rejected compiler-safe groups: zero;
- second exact-VLM acceptance: zero.

The last line is intentional: a successful code mutation does not receive an
acceptance privilege. Both repaired candidates were re-rendered and rejected
again by the exact VLM.

r126 remains **fail**:

- 10/20, ten BOOK operations, seven visual languages;
- five p.3 scopes and two ground strategies (five notches, five lifts);
- downstream legal/retention/parking 10/10 pass;
- portfolio failures: family resemblance, repeated footprint, repeated roof,
  weak program language, fragmented Lego and too few candidates.

Original-resolution PNG review agrees. Candidate 7 reads as a thin sawtooth
wall, candidate 2 is over-layered, and multiple compact blocks differ mainly
by roof cap. The portfolio feedback requests three spatial logics: a carved
street bar, a split-wing/court, and a lifted or bridged public ground.

r127 therefore adds `split_wing` only as a post-BOOK, access-bound program
relation on a simple continuous body, with a low ground spine that preserves
one occupiable solid. It is not added to complex/distributed bodies, not added
to gym when absent from its contract, and does not alter the universal bank or
BOOK. All 192 three-program projections remain connected and the full
geometry-language suite remains 154/154. r127 is active; judge its final board.

### r127 split-wing experiment rejected

r127 completed with zero stderr and remained **fail**: 10/20, nine BOOK
operations, eight visual languages, five p.3 scopes and only notch/lift ground
strategies. The exact portfolio VLM reports family resemblance, repeated
footprint/roof, weak program language, box dominance and too few candidates.
No post-BOOK split-wing or court survived final VLM selection. Direct PNG
review shows the same compact caps plus thin sawtooth-wall candidates.

This is negative evidence, not a feature to retain. The temporary split-wing
program projection was removed. The stable code keeps the r124 court/lift/
notch relation system and the proven cross-kind mutation repair. The remaining
blocker is not another terminal macro quota; it is the base/BOOK shortlist and
visual-review policy admitting roof-cap variants while failing to retain enough
coherent spatial families. Do not add more use-specific body macros to program
projection to chase a count.

### r128 concept-stratified shortlist experiment rejected

r128 kept the r124 geometry rules and changed only VLM shortlist strata to
include final ground/body strategy. It completed with zero stderr but regressed
to 9/20, eight BOOK operations, seven visual languages, five scopes and two
ground strategies. Base-stage VLM hard passes fell from r126's 19 to 16, and
the final portfolio still failed family resemblance, repeated footprint/roof,
weak program language and too few candidates. No open court survived.

The shortlist change was removed. Stable production code now retains only the
changes with positive causal evidence: universal-before-program ordering,
explicit post-BOOK court/lift/notch program relations, connectivity guards,
and compiler-safe cross-kind typed repair. r127/r128 remain artifacts and
memory evidence, not code paths to accumulate.

## 2026-07-20 activation graph + structured MASS PNG memory

The two supplied KakaoTalk PNGs were inspected again at original resolution.
Their useful principle is a dense activation field with branching, crossing,
recombination and one luminous current lineage. They are not architectural
forms to copy and are not evidence of model-internal neurons.

The `/design` BOOK-language frontend now projects the actual typed manifest as:

`BOOK source -> universal base seed -> chassis -> p.3 Base Volume -> orientation -> BOOK principles -> variations -> program projection -> site-derived FAR alternative -> hard gates -> VLM -> portfolio memory`.

Browser evidence:

- `ARR/frontend/maas-language-flow-final.png`;
- `ARR/frontend/maas-agent-node-flow-final.png`;
- deterministic SVG relation edges plus a luminous current lineage;
- fullscreen left/right inspection passed, page errors zero and Vite error
  overlay absent;
- Base Volume displays `1/1`, `3/8`, `1/2`, `1/4`, `1/8`, `1/16` separately
  from the five host proportions;
- the UI is a human/audit projection of backend data. Clicking a layer changes
  frontend emphasis only; it does not silently mutate generation.

The frontend contains two different graphs and they must not be conflated. The
BOOK activation field explains the architectural search language. The AG-light
node graph explains runtime responsibility and evidence handoff. Its previous
single vertical chain was replaced by a typed topology module: law/parking
evidence and BOOK/LLM/MassDSL language branch in parallel, converge at MAAS
geometry, pass through grammar/final review, and return only a bounded revision
edge to the orchestrator. The browser contract exposes nine nodes, ten semantic
edges and explicit edge labels; page errors remain zero.

The language manifest previously reused the three-case portfolio benchmark
catalog (`neighborhood`, `gymnasium`, `cultural`) and therefore understated the
real program support. It now reads the authoritative `program_profiles.v1`
catalog: housing, cafe, gymnasium, office, cultural, neighborhood living and
retail. The program-conditioned lattice is 5,259,870 paths. Four site-derived
capacity alternatives then produce 21,039,480 theoretical pre-gate paths:
spatial reserve, balanced yield, brief target and maximum feasible. This is a
semantic lattice count, not a claim that every path is rendered or sent to a
VLM; generation deterministically stratifies the existing candidate budget
across these alternatives. The 82 universal geometry programs remain
program-independent; program and capacity are downstream projections.

The capacity boundary is intentionally modular and fixed by tests:

- `capacity_policy.py` resolves use/brief intent;
- `capacity_contract.py` measures the parcel's feasible maximum;
- `book_language/capacity_alternatives.py` derives the four monotonic targets;
- generation, selection, exact typed VLM repair, MASS render memory and the
  frontend consume that contract rather than authoring another typology.

Targets are derived from the actual feasible-capacity contract, not parcel
coordinates or finished-form geometry. The same typed form language is fitted
against the selected target and then faces six downstream hard gates, including
an explicit FAR-alternative target gate. A VLM typed repair must preserve and
remeasure its parent alternative instead of silently returning to a default.

ArchDaily/VLM evidence is real rather than a UI label:

- default workspace corpus: 13 ArchDaily metadata collections and 375 local
  images at this checkpoint;
- retrieval order: program-matched retrieval -> whole-building image audit ->
  relation-only author context -> exact post-program/post-BOOK VLM critic;
- completed precedent form is never geometry authority;
- effective default live guards shown by the manifest: two reference images
  per request, process cap 24, one retry;
- no live VLM call was launched while implementing or verifying this frontend
  and memory update.

Structured memory is now explicit. After a MASS board is rendered, every
selected candidate is appended to the portable outcome graph as a
`render_artifact` record bound by `program_hash`, `projected_program_hash`,
`geometry_hash`, BOOK principle/scope, board PNG, exact card `crop_box` and
measured material-pixel evidence. Graph edges are:

`projected_geometry_program -[rendered_as]-> render_artifact -[measured_by]-> outcome`.

The PNG is human/VLM observation evidence only. Typed AST + program hash +
geometry hash remain mutation authority. Current machine-readable snapshot:

- `docs/ai-session-memory/maas-language-visual-memory-current.json`;
- exporter: `python manage.py export_maas_language_memory`;
- exporter never launches live VLM.

Cost/run truth: the interrupted r161 live process was stopped after producing
356 candidate preview PNGs. It produced no completed program board, combined
board or summary, so it is neither a pass nor a visual result. Do not infer
quality from those previews or restart a paid live run without explicit user
approval. The next MASS validation should be a bounded fresh run, then inspect
the final board PNG at original resolution and let the same render artifact
contract persist its exact visual evidence.

The latest *completed* MASS visual evidence is therefore r160, not r161. Its
1920 x 1360 neighborhood board has SHA-256
`4e446663ee6358d1c2a92f60bd6e1e9e64ab596365f8020ae6db27e41b0d9898`.
It selected 16/20 candidates, 13 BOOK operations, 13 measured visual languages
and all six p.3 Base Volume scopes. Downstream hard gates pass, while numeric
status and the portfolio VLM hard gate fail. Recorded failures are insufficient
candidate count, portfolio visual-diversity failure and roof-archetype
repetition above 30 percent.

r160 predates the capacity-alternative contract and records no ALT identity or
target achievement. It must not be presented as visual proof of the four new
alternatives. A new bounded run and original-resolution board review are still
required before claiming their MASS visual quality. No live VLM run was started
for the capacity implementation or frontend verification.

Direct original-resolution review agrees with that fail: four slots are visibly
empty; compact block, courtyard and roof-cap relatives remain dominant. There
are some skewed, curved, layered and cross-like alternatives, but they do not
close portfolio-level diversity. This board is diagnostic MASS PNG evidence,
not a competition-grade result. The normalized machine checkpoint is
`docs/ai-session-memory/maas-latest-mass-visual-evidence.json`. It preserves a
link to the legacy 19 MB run summary instead of rewriting it; that old summary
contains unescaped line breaks in nested strings and is not accepted by a
standard JSON parser.

Verification for this checkpoint:

- frontend TypeScript check: pass;
- frontend Vite web build: pass (existing large-chunk warning only);
- real browser `/design`: pass, no page errors;
- language manifest test: pass;
- structured language-memory and MASS PNG outcome-memory tests: 2/2 pass;
- `git diff --check` on the touched focused files: pass.

### 2026-07-20 FAR-alternative preflight r163-r167

The first isolated non-live capacity board, r163, proved that all four ALT IDs
were generated, but exposed a real selection bug: all 20 selected candidates
missed their own target even though the generation pool contained measured
target passes. Diversity scoring was allowed to outrank the advertised
capacity hard gate. The final selector now keeps target misses available for
typed VLM repair but rejects them before portfolio diversity scoring.

r164 and r165 both completed against the same real PNU without live VLM. Their
selected cards are now honest measured passes: 10/10 selected cards meet the
requested ALT target, mean target utilization is 0.75, mean achieved
utilization is 0.7766 and mean FAR is 122.9585 percent. The selector measured
350 final-pool candidates, retained 17 target passes and rejected 333 misses.

This is still a portfolio failure, not completion. Only 10/20 candidates can
coexist under the unchanged silhouette/family caps; only five of six p.3 base
scopes and three of four ALT bands survive. `maximum_feasible` is absent, BOOK
operation/visual-language counts are six, and roof repetition still exceeds
the cap. Direct review of the 1920 x 716 r165 board shows compact box, shallow
court and notched-block relatives still dominating. Its exact evidence is:

- `docs/playwright/design-route-live-verify/book-program-portfolios-r165-capacity-host-reachable-preflight-neighborhood/maas-book-neighborhood-20.png`;
- SHA-256 `70c1440b08686ef2656c2d30d685c577763caf8137aed557f5a59bcabf064321`;
- normalized checkpoint in `maas-latest-mass-visual-evidence.json`.

The host-reachability guard added for r165 is a general mathematical defense:
an upper sunlight section is not assigned a target above its host-area × floor
count upper bound. It did not change this PNU's result, so do not cite it as an
improvement. The remaining work is to increase genuinely target-passing form
supply, especially maximum-feasible, without weakening the 0.98 target or the
measured diversity caps.

r166 added one bounded, evidence-derived capacity feedback step. After the
first materialization misses its ALT target, the capacity module derives one
new plan-coverage request from `target / achieved` and rematerializes the same
typed AST once. It does not add a form macro, change a target, loop randomly or
bypass downstream gates. Of 1,601 attempted retries, 239 improved measured
capacity. The final visual pool's target-passing supply rose from 17 to 60 and
the board rose from 10/20 to 20/20, with 17 BOOK operations, 12 measured visual
languages and all six p.3 base scopes. All 20 selected cards pass their own ALT
target; mean target is 0.745, mean achieved is 0.7475 and mean FAR is 118.343
percent.

r167 added rare-ALT preservation in the bounded visual-review transport pool.
It produced a byte-identical board to r166, proving that the remaining
`maximum_feasible` loss happens before that bounded pool, not in its score
ordering. Three maximum-feasible candidates pass the early capacity measure,
but none survives the program/concept supply boundary. The current board still
fails overall because it contains only spatial reserve, balanced yield and
brief target, and one required design-concept controller remains missing.

Latest exact capacity evidence:

- `docs/playwright/design-route-live-verify/book-program-portfolios-r167-capacity-four-alt-preflight-neighborhood/maas-book-neighborhood-20.png`;
- 1920 x 1360, SHA-256 `1576947f7ff4cc2bfc569ead1bbbf575276fb25169f6e65966abefd04f95eca8`;
- 20/20 measured capacity passes, but only three of four ALT bands;
- normalized checkpoint in `maas-latest-mass-visual-evidence.json`.

Direct original-resolution review shows materially more terraced, bent, split
and court variants than r165, but compact block relatives remain common. This
is a useful capacity-closed-loop preflight, not final aesthetic or live-VLM
acceptance. No paid/live VLM request was made in r163-r167.

### 2026-07-20 BOOK execution-graph architecture r169

The frontend "form authority" diagram was replaced because it conflated BOOK
source pages, engineered Base Seed/Chassis helpers and executable search
stages, then drew synthetic modulo-based links. The authoritative manifest is
now `arr.maas.language_system.v3` with
`arr.maas.book_exploration_graph.v1`. Its only public exploration roots are the
six p.3 Base Models: 1/1, 3/8, 1/2, 1/4, 1/8 and 1/16. Base Seed proportions
and Chassis relation scaffolds remain available only through `detail` edges
under a selected Base Model.

The BOOK graph declares 229 total nodes and 1,065 total edges. The frontend
renders only the 130 visible exploration nodes and the 643 explicit execution
or feedback edges; it does not calculate or display a Cartesian path count.
The ordered executable lane is Base Model, orientation, operation family,
cardinality, 30 BOOK operatives, variation, 20 combinations plus nine
aggregations and one separately marked experimental gateway, program,
four capacity alternatives, hard gates, MASS PNG, VLM and accepted memory.
The 69 scans and four extracted BOOK source documents are evidence nodes, not
actions.

The inspectable route is `http://127.0.0.1:5175/design/language`. The `/design`
page embeds a compact instance. The UI is monochrome and draws only backend
edge IDs. Selecting a Base Model shows the exact p.3 scan and can expand its
engineered internal instantiation. The second tab reads a bounded directional
slice from the real append-only outcome graph; the verified PNU slice is five
compiled geometries, one candidate outcome, one portfolio VLM critic and one
portfolio node connected by seven stored edges.

Evidence screenshots:

- `docs/playwright/design-route-live-verify/r169-book-exploration-graph/book-language.png`;
- `docs/playwright/design-route-live-verify/r169-book-exploration-graph/candidate-lineage-final.png`.

The 69-page image audit now has a content-addressed VLM cache contract and a
cost-guarded management command. Its default is status/cache-only and makes
zero API calls. Live requests require explicit pages (or `--all`) and the exact
confirmation phrase `BOOK-69-LIVE`. At this checkpoint the audit cache is
truthfully 0/69; no paid VLM request was made. Browser verification found 13
BOOK stages, 130 visible nodes, 643 real edges, the p.3 evidence image and zero
console/runtime errors. Four focused Django tests, Django system check and
frontend TypeScript check pass. Vite web production build passes; the wider
Electron packaging command continues to fail later on the pre-existing missing
`build/icon.ico`, after the web build has completed successfully.

### 2026-07-20 complete BOOK authority + Extended CSG contract r170

The four architect-supplied extracted BOOK documents are now one mandatory
source bundle rather than optional globbed references. Their roles are fixed:
the CSV is the complete addressable vocabulary catalog; the full-extraction
MD preserves page structure and printed relations; the term-index MD
normalizes architectural vocabulary; and the page-index MD binds meaning to
page evidence. The current bundle is four documents and 1,346 physical lines
including the CSV header. The catalog loader hard-fails unless all four exist
and the CSV still resolves exactly 134 entries with layer counts 29 process,
11 diagram, 30 operation, 20 combination, five aggregation methods, nine
aggregation expressions and 30 implementations.

The recursive mass system is explicitly published as
`arr.maas.extended_csg_contract.v1`. Its public invariant is: a rectangle
profile/box Base Model enters a typed recursive SolidNode program; transform,
modifier, Boolean, pattern, composition and architectural macro results all
return SolidNode; compiler output is a manifold solid and mesh; four-view MASS
PNG is then available to the typed VLM critic and hard GATE. Profiles and paths
are immutable parameters, not free mesh-editing nodes.

All 18 requested mass phenotypes are executable ASTs, not frontend labels.
They compile to 18 distinct geometry hashes and all pass the unchanged
geometry GATE. The GATE now records signed mesh volume and checks outward
normal winding explicitly. Candidate normalization keeps stable edit-target
IDs in the program hash but also has an alpha-renaming-independent structural
hash; ProgramCost now includes tree-depth penalty in addition to node,
Boolean, deformation, topology, unstable-operation and invalid-geometry
penalties.

The frontend route remains `http://127.0.0.1:5175/design/language` and now has
four modes. `BOOK 전체 언어 134` exposes every source entry and shows, for
example, Taper connected to Taper+Taper, Embed+Taper, Taper+Bend, Array·Taper
and the implementation evidence. `사각형 → 18 MASS` reads the actual backend
AST graph from Base Model through core operators, macros, all 18 phenotypes,
compiler, render, Reference VLM, GATE and canonical selector. Selecting a
phenotype displays its actual compiled four-view 900 x 680 MASS PNG, exact DSL,
geometry hash, implementation recommendation and numerical risk. Preview
assets are geometry-hash cached; this work made zero paid/live VLM requests.

Browser evidence:

- `docs/playwright/design-route-live-verify/r170-extended-csg-18-mass/book-language-134-wide.png`;
- `docs/playwright/design-route-live-verify/r170-extended-csg-18-mass/geometry-language-18-mass-with-preview.png`.

Verification: focused Django contract tests 4/4 pass; legacy recursive DSL,
18-phenotype, L-equivalence and modifier-order tests pass; frontend TypeScript
check passes; Vite web build passes with only the existing chunk-size warning;
browser route has content, 134 catalog nodes, 18 phenotype nodes, a loaded
900 x 680 compiled preview and zero Vite error overlays.

### 2026-07-20 per-MASS execution passport + causal activation graph r171

Every recursive Geometry MASS now owns one auditable execution passport with
schema `arr.maas.mass_execution_passport.v1`; this is not limited to a 20-card
PNG board. The stable `mass_id` is derived from the exact geometry/program
hash. Its ordered stages are Base Model, BOOK projection, recursive Geometry
AST, use/program projection, site, FAR/capacity alternative, legal hard gate,
parking hard gate, program-fit gate, compiler, Geometry GATE, four-view PNG,
VLM critic and final selector. A missing downstream input is always stored as
`not_evaluated` and can never be counted as a pass.

`geometry_language/execution_passport.py` is the separate modular authority.
`CompilationResult.to_dict()` embeds the initial passport; every
`render_compilation_preview()` writes a `.png.passport.json` sidecar; the VLM
adapter updates that same sidecar with the exact cache/live response; the
SourceMass bridge preserves it; and downstream legal/parking evaluation adds
the actual candidate results before portfolio selection. Portfolio geometry
artifacts expose the enriched passport as both
`properties.mass_execution_passport` and
`geometry_artifact.executionPassport`.

The activation graph is an observable causal trace, not a claim to expose
hidden model neurons. AST dependencies, compiler trace, GATE, four rendered
views, downstream gates, VLM concept scores, typed repair requests and selector
are drawn only when their evidence exists. The UI uses the architect-supplied
neural-network references as a monochrome visual precedent: vertical node
fields, thin real edges, white evaluated activity, grey unevaluated activity,
and one restrained amber selection. Select any phenotype at
`http://127.0.0.1:5175/design/language`, then open `FULL FLOW` in the execution
passport panel. Actual newly generated `/design` candidates also render the
same card immediately below `DesignInspector` whenever their GeoJSON
properties contain `mass_execution_passport`; this is the per-result inspection
path, while the language route remains the engine/phenotype inspection path.

The phenotype API is
`/design/maas/geometry-shapes/<index>/passport/`. Browser verification of the
route returned HTTP 200, meaningful content, 14 execution stages, no Vite
overlay and no console/page errors. Evidence screenshots:

- `docs/playwright/design-route-live-verify/r171-mass-execution-passport/mass-passport-panel.png`;
- `docs/playwright/design-route-live-verify/r171-mass-execution-passport/mass-passport-full-flow.png`.

One deliberately bounded live VLM request was made for Courtyard Mass:
reference audit images 0, candidate requests at most 1, model
`gpt-5.4-mini`, cache miss, response ID present, eight real concept scores and
zero typed edits. The sidecar is
`shape-05-vlm.png.passport.json`; examples include void publicness 0.88,
hierarchy 0.84 and section/program fit 0.58. This supersedes r170's statement
that no live request had been made.

Verification: focused Django execution-passport/Extended-CSG tests 7/7 pass;
SourceMass passport hash continuity passes; mocked boundary integration proves
the real legal/parking return values activate the same passport; frontend
TypeScript check and Vite web build pass; browser click-through passes. The
broader program-massing suite and an unmocked diagnostic parking layout both
exceeded their 180 s / 60 s diagnostic limits without output, so they are not
reported as passing. The existing parking search performance remains a
separate follow-up; the passport truthfully records its result whenever it
finishes.

### 2026-07-20 Agent-consumed causal flow + surface-connected MASS r173

The graph visible in the frontend is not treated as a hidden neural network.
The same per-MASS causal trace is now compressed into
`arr.maas.mass_execution_agent_context.v1`, with exact AST node IDs, active
edges, stage statuses, failed stages, required-but-pending stages, editable
nodes, protected nodes, VLM concept scores and typed geometry edits. The live
VLM Critic receives this context in its prompt; Critic output carries it into
typed Repair; the loop trace persists it; Selector ranks fewer failed stages
before visual score; and a subsequent Author prompt can receive the prior
candidate context. An initial Author cannot know a future execution graph and
is not falsely reported as doing so.

The VLM prompt contract is now
`arr.maas.vlm_prompt.site_visible_program_concept_graph_edit_geometry_edit.v20_mass_execution_agent_context`.
This invalidates older cache answers that never saw the Agent context. No new
paid VLM request was made for r172/r173. The one real r171 VLM response proves
image recognition under v19, not live v20 graph consumption; v20 is proven by
the prompt path and focused tests until an explicitly authorized live call is
made.

The former 590-line execution-passport module was split into evidence,
activation, persistence and Agent-context modules; the facade is 167 lines.
`portfolio_benchmark.py` is 1,769 lines and delegates passport assembly through
`mass_passport_bridge.py` instead of growing another inline block. Atomic
sidecar persistence preserves matching VLM/legal/parking evidence on rerender
and now stores `agent_context` beside every exact PNG.

Direct inspection found a real geometry contradiction in r172: BOOK arrays
compiled, but post-BOOK program notches/split wings could leave 2-3 components
even after a node called `join_related`. Its implementation joined bounding-box
centres, which can lie in the empty centre of U/courtyard/tapered bodies. r173
replaces this with a kernel-surface shortest-pair connector embedded into both
solids. The five representative BOOK+program masses now compile and hard-gate
with one component each and five distinct geometry hashes: expand, bend,
taper, embed+taper and taper+array. Array remains visually dense by design but
is one physical solid rather than disconnected fragments.

Evidence directory:

- `docs/playwright/design-route-live-verify/r173-agent-aware-connected-mass-flow/summary.json`;
- `docs/playwright/design-route-live-verify/r173-agent-aware-connected-mass-flow/aggregation_array_taper.png`;
- `docs/playwright/design-route-live-verify/r173-agent-aware-connected-mass-flow/frontend-tapered-full-flow.png`.

Browser verification at `http://127.0.0.1:5175/design/language` returned 200,
no Vite overlay and zero console/page errors. Selecting Tapered Tower exposes
its compiled PNG, exact DSL and FULL FLOW passport with BASE, GEOMETRY,
PROGRAM, COMPILER, GATE, SITE, CAPACITY, LAW, PARKING, PROGRAM FIT, RENDER,
VLM and SELECTOR. Focused execution/Extended-CSG tests pass 10/10; five
Author/Critic/Repair/Selector regressions pass 5/5; prompt-context regression
passes; Python compilation and frontend `tsc --noEmit` pass.

Honest boundary: r173 has no parcel input and made no paid v20 call, so site,
capacity, law, parking, program-fit, VLM and selector remain `not_evaluated` in
the representative sidecars. They are visible as pending, never as pass. The
known unmocked parking-search performance issue is still open.

### 2026-07-20 Selected MASS trace + exact VLM image provenance r174

The Geometry Language frontend now treats a derived MASS as the primary
inspection object. The right evidence sidebar defaults to the selected MASS
and its deterministic four-view PNG. A bottom strip exposes all 18 compiled
MASS results; clicking a thumbnail switches the sidebar and filters the graph
to the exact directed derivation path for that MASS. Unrelated edges are not
rendered. BOOK mode also retains one choice per stage and draws only the
shortest directed path between the selected anchors.

The selected phenotype adds a real final-result node after Selector. Both the
geometry graph and the per-MASS FULL FLOW therefore end in an image-backed MASS
node. The activation passport also distinguishes `render:mass_png` from
`result:mass`, with incoming `generated_result` and `selection_status` edges.

VLM precedent provenance is now input-exact. A live scorer records only the
candidate image and reference images actually sent to the model, including
input order, source/source URL, local path, SHA-256, selection role, matched
tags and program-match metadata. Those used references become image nodes with
`visual_reference_input` edges into the VLM Critic and are copied into the
Agent-consumed causal context. Retrieved-but-not-sent candidates never appear
as active reference nodes. Local images inside the curated reference corpus
have an allow-listed `/design/maas/reference-assets/...` preview route; a real
ArchDaily cache JPG returned HTTP 200 and `image/jpeg`.

Browser verification selected `10 Tapered Tower`: 18 gallery results, one right
sidebar MASS, one final graph preview node, 10 selected-path edges, and two
image nodes in FULL FLOW (`render:mass_png`, `result:mass`). Console, page and
request error counts were zero. Focused backend tests pass 10/10 and frontend
`tsc --noEmit` passes. Evidence:

- `docs/playwright/design-route-live-verify/r174-selected-trace-reference-results/selected-tapered-tower-sidebar-gallery.png`;
- `docs/playwright/design-route-live-verify/r174-selected-trace-reference-results/full-flow-result-mass-node.png`;
- `docs/playwright/design-route-live-verify/r174-selected-trace-reference-results/verify-selected-mass-flow.json`.

No new paid VLM request was made. Consequently the current parcel-free Tapered
Tower passport truthfully contains zero VLM reference nodes; they will appear
only on a future actual VLM pass that sent references.

### 2026-07-20 Actual executed MASS results replace static examples r175

r174's Geometry tab was visually functional but used the static 18-shape
language examples. That is not an actual generation result and is superseded
for the MASS inspection UI by r175.

The `실제 실행 MASS` tab now loads the newest completed portfolio archive,
currently
`book-program-portfolios-r168-capacity-language-decoupled-preflight-neighborhood`.
Its 20 selected candidates are read from
`maas-book-exact-geometry-artifacts.json` and correlated with the 20 summary
rows. The gallery and right sidebar use the exact archived candidate crop from
the original board. They do not use BOOK scans or `/geometry-shapes/*` example
renders.

Each candidate is bound to `variant_id`, source sequence, archived Geometry
Program, program hash, geometry hash, PNU, capacity alternative, FAR, legal,
parking, program-fit and selector evidence. The central graph comes from that
candidate's activation passport. Clicking another candidate changes the PNG,
DSL, hashes and graph; the standalone layout keeps the graph, right evidence
and bottom 20-candidate strip visible together.

Original archive evidence is authoritative. Two archived programs (08 and 10)
produce different geometry hashes under the current compiler, so their actual
PNG is served directly from the archived board rather than silently replacing
it with a new replay. The passport rehydrates the original compilation metrics
and original combined hard-gate result and labels its authority accordingly;
current-only metrics absent from the archive are not fabricated.

Verification:

- 20/20 candidate PNG endpoints return HTTP 200;
- 20/20 passport geometry hashes match the archive;
- 20/20 recorded Geometry/Site/Capacity/Law/Parking/Program/Selector gates pass;
- 20/20 VLM stages remain honestly `not_evaluated` for this no-live-VLM run;
- after entering the actual MASS tab, `/executed-masses/*` is used while
  `/geometry-shapes/*` and `/book-assets/*` request counts are both zero;
- browser errors are zero; focused backend tests pass 10/10 and frontend
  `tsc --noEmit` passes.

Evidence:

- `docs/playwright/design-route-live-verify/r175-actual-executed-mass-results/actual-maas-02-sidebar-gallery-trace.png`;
- `docs/playwright/design-route-live-verify/r175-actual-executed-mass-results/actual-maas-02-full-flow.png`;
- `docs/playwright/design-route-live-verify/r175-actual-executed-mass-results/verify-actual-executed-masses.json`;
- `docs/playwright/design-route-live-verify/r175-actual-executed-mass-results/passport-20-audit.json`.
