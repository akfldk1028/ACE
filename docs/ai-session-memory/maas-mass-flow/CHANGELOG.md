# MASS Flow Memory Changelog

## 2026-07-21 — r182

- Created this modular memory folder.
- Replaced fake replenishment with actual recursive Geometry Program pages.
- Reached 20/20 on page 7 without relaxing geometry/legal/parking/capacity or
  silhouette gates.
- Unified selector and benchmark availability around the capacity-target
  hard-pass universe.
- Verified one frontend graph, actual MASS 20/20 image loading, selected path,
  ArchDaily retrieval truth and zero BOOK raster evidence.
- Recorded that Live VLM was not run and ArchDaily actual input count is zero.
- Recorded site-specific PNU legal preflight and its non-approval-grade limits.
- Recorded MASS 17's visually floating layered silhouette as an open issue.
# 2026-07-21 — Base-Model-relative parametric invariant

- Promoted Base-Model-relative authoring to a non-negotiable MASS invariant.
- All actual placement/dimension resolution must come from evaluated local
  bounds, axes, face frames and topology.
- Absolute completed-form/image-specific coordinate hardcoding is forbidden.
- Named dimensionless sampling policies remain allowed only as replaceable,
  explicit Geometry Program inputs.
- Added the `host_face_normalized.v1` contract for parametric wall/top/bottom
  guest attachment.

## 2026-07-21 — Controller authority, archive and shared agents

- Separated typed BOOK operation provenance from program/use controller
  authority. In particular, `book_split` can no longer satisfy public-space or
  access-controller gates by name alone.
- Preserved the bounded r184 Live VLM attempt as `0 MASS · NO FINAL` after 24
  paid calls; no older successful run is relabelled as r184 output.
- Added one chronological run graph and MASS-only archive to `/design/language`.
  The browser verification sees 90 runs, replays r182's 20 actual MASS renders,
  loads no BOOK raster and reports no console/page errors.
- Defined `ARR/backend/agents` as the shared control plane and shared-memory
  parent for sibling agent repositories. `elevationAgent` is a folder-structure
  reference only; future `massAgent` gets new identity and private memory.
- Recorded hash-bound handoffs for later plan, elevation, facade, law, critic
  and selector collaboration.
