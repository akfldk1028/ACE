# Design Orchestrator Rules

1. Preserve the canonical flow:
   `law_graph_agent -> parking_agent -> llm_architect_agent -> massdsl_agent -> maas_geometry_agent -> grammar_critic_agent -> review_agent`.
2. Keep ARR backend, JSON_MODULES, and AG-light in sync.
3. Route parking/legal/orderliness failures back into revision, not final pass.
4. Do not collapse specialist responsibilities into one prompt.

