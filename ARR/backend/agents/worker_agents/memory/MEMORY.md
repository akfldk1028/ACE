# ARR Worker Agents Memory

## 2026-07-07

- Added a GitAgent-style module folder standard for global workers.
- Kept current executable adapters in `implementations/` to avoid import churn.
- `flight_specialist_worker` is the first mirrored worker module.
