# MAAS Geometry Agent Memory

Created: 2026-07-07

The user repeatedly rejected massing that looked like arbitrary boxes. This agent must make geometry generation rule-based and evidence-bearing.

Current handoff:

`massdsl_agent -> maas_geometry_agent -> grammar_critic_agent`

## 2026-07-24 - UnitBox causal MASS and render boundary

- Geometry authority starts from one `UnitBox` and relative 4x4 transforms.
  Derived fractions are measurements of that live host, never independent
  primitive catalogs or world-coordinate recipes.
- BOOK is typed operation language, not raster evidence. A generated MASS must
  retain the selected causal path from UnitBox -> derived volume -> orientation
  -> operation/cardinality -> BOOK operative -> compiler/gates -> render/VLM.
- `radial_cross` is compiled from one wing transform, a bounded radial array,
  and one common hub union. Detached wings, decorative fragments, and
  archive-only hash variation are invalid.
- BOOK `stack.upper_ratio` is executable geometry. The adapter inverts the
  compiler rule `topScale = 1 - setback * (levels - 1)` instead of using a
  constant setback.
- Aesthetic generation belongs to the separate elevation/render agent. It may
  texture the locked MASS silhouette but cannot change program/geometry hashes.
  The top panel remains roof evidence and passes the roof semantic guard.
- Frontend evidence order is compiler MASS, architectural render ALT, then
  compact six-view geometry verification. The bottom archive shows MASS only.
- Verified batch `r230-radial-roof-render`: 10/10 fresh executions, 10 unique
  program hashes, 10 unique geometry hashes, no rejected candidates.
- Paid image proof was intentionally limited to two one-shot calls:
  `r230-...-03-radial-cross` and `r230-...-04-stepped-setback`, both
  `gpt-image-2`, one request, zero retries, roof guard applied.
- Browser proof:
  `docs/playwright/design-route-live-verify/r230-radial-selected-render.png`.
  Selecting MASS 03 exposes one render card, 24 MASS-only archive cards, no
  console errors, and no Vite error overlay.

## 2026-07-26 - immutable MASS / creative facade enforcement

- MASS03 identity remains
  `program_hash=8c0f2d94...15050a`,
  `geometry_hash=6d6cea4d...1179a`. Creative facade work may not change either.
- The locked technical front/right/back/left views are authority for silhouette,
  height, storey count and component boundaries. A facade critic that sees only
  generated outputs is insufficient.
- The current creative four-view proposal is visibly available in the frontend
  but remains failed; this does not change MASS geometry-ready status.
- User cost boundary is a small test only. MASS04 and further paid generation
  require fresh approval.

## 2026-07-27 - authoritative modular full-flow contract

The maintained production path is:

`site/law/program graph -> law-derived floor-capacity plan -> UnitBox/BaseVolume
1/1..1/16 -> BOOK typed operators -> LLM-authored GeometryProgram/AST ->
homogeneous 4x4 transforms + boolean compilation -> shared floor contract ->
law/FAR/BCR/parking/capacity gates -> bounded paid VLM review/typed repair ->
selected immutable MASS passport -> elevation agent -> optional one-shot GPT
Image material proposal -> frontend evidence`

Non-negotiable boundaries:

- Storey count is derived from legal height, program capacity and floor-height
  constraints. Five storeys is a test fixture, never a global rule.
- BaseVolume identity and every translate/rotate/scale operation compile through
  homogeneous 4x4 matrices. Booleans and topology checks remain explicit
  geometry operations; a matrix does not replace union/difference/intersection.
- `floor_capacity_plan.py` owns law/program-derived target floor allocation.
  `shared_floor_contract.py` owns the exact accepted plates, floor guides,
  measured areas and the bound selectable capacity band. MASS, replay and
  elevation must carry the same contract and hashes.
- `capacity_routing.py` filters measured selectable capacity before paid VLM.
  Requested aspirational capacity remains visible as evidence, but it cannot
  erase a lower measured band that is still a valid legal/selectable result.
- VLM reviews rendered MASS and returns typed scores/repairs. It does not invent
  geometry, law values, floor count or area. GPT Image is elevation/material
  exploration only and must preserve the selected MASS silhouette and floor
  guides.
- `single_execution/replay.py` is an exact replay path, not regeneration. It
  must preserve geometry hash, stage status and shared-floor evidence before
  handing the MASS to elevation.
- The frontend route `/design/language` is evidence UI for the one selected
  causal execution. Archive thumbnails are alternatives/history, not multiple
  masses inside the selected result.

Latest bounded proof:

- Portfolio `book-program-portfolios-r273-vlm4-diverse-reserve` used four paid
  VLM review slots, selected one legal/parking/VLM-passing MASS and did not run
  a hundreds-of-images sweep.
- Exact replay `r273-selected-exact-replay-v3` preserves geometry hash
  `5e6cb5711d30e4411effcb700bda10a571d553974dc74813de0c3bf3ee25c22e`
  and floor guides `[0.0, 3.5, 7.0, 10.5, 14.0]`.
- The selected four-storey result measures GFA `233.713 m2`, FAR `88.485%`,
  BCR `27.348%`, and parking `2/2`. Its measured selectable band is
  `spatial_reserve` at `70.33%`; the requested `90%` brief target was not met.
  Therefore this is a full-flow integration proof, not yet a
  competition-grade final design.
- One `gpt-image-2` elevation proposal was generated with one request and zero
  retries. The silhouette lock passed and pixels outside the edit mask remained
  unchanged.
- Browser proof at port 5178 loaded the selected MASS, six technical
  elevations and the one generated elevation proposal with no broken images,
  console errors or Vite overlay.

## 2026-07-27 - r273 PNG audit correction and unresolved root cause

This section supersedes any earlier description of r273 as a complete
generative proof. It is an orchestration/replay/elevation integration proof,
but it did **not** exercise the intended LLM-authored MASS path and it did not
produce a competition-grade selected MASS.

Directly reviewed evidence:

- `book-program-portfolios-r273-vlm4-diverse-reserve/maas-book-programs-60-summary.png`
- `single-executions/r273-selected-exact-replay-v3/mass.png`
- `single-executions/r273-selected-exact-replay-v3/elevation/views/front.png`
- `single-executions/r273-selected-exact-replay-v3/elevation/views/axon.png`
- `single-executions/r273-selected-exact-replay-v3/elevation/proposals/alt-01/proposal.png`

Observed visual failure:

- The selected MASS reads as one long carved/extruded monolith with slots, not
  as a convincing floor-aware architectural composition. The technical
  elevations expose four floor guides, but the mass hierarchy remains three
  large stacked blocks and the section strategy is `none`.
- The portfolio board contains one accepted card and four empty
  `NO DISTINCT HARD-PASS CANDIDATE` positions. The card itself reports
  `BRIEF 90% > 70%` and `FAR 88/126%`.
- GPT Image added credible concrete/glazing and floor rhythm, but it correctly
  preserved the locked weak silhouette. It cannot repair MASS geometry.

Measured selection conflict:

- The law-derived four-floor plan was reachable: target GFA `299.089 m2`
  at `90%` of feasible `332.322 m2`.
- Thirty-two candidates passed the downstream legal/parking pool and four were
  reviewed with paid VLM. A `311.494 m2` candidate reached `93.73%`, but VLM
  rejected it as too box-like and the wrong program typology.
- The only VLM hard-pass candidate was `233.713 m2`, `70.33%`, FAR `88.485%`.
  Therefore the selector chose visual hard-pass at the minimum capacity band
  instead of finding the required intersection of target capacity and visual
  quality.
- Typed repair changed two failed candidates. One then failed shared-floor
  clear-depth; the other failed capacity-target routing, so no repaired
  candidate reached the second paid VLM review.

Missing intended agent participation:

- `geometry_program_llm_author_active_seed_count = 0`.
- All `164` geometry author statuses were `not_requested_pre_program`.
- Paid VLM shortlist contained `0` LLM-authored candidates.
- `mass_brain_geometry_trace` timed out and had `selection_effect =
  none_shadow_only`.
- The selected result was a deterministic universal-form/BOOK path
  (`agent_notch` + `book:operative:embed`), not an LLM-authored
  GeometryProgram/AST.

Elevation handoff evidence gap:

- The r273 passport records requested `brief_target` and selectable
  `spatial_reserve`, but its elevation condition pack contains
  `capacity_alternative = {}` and `target_floor_areas_m2 = []`.
- The exact floor guides and geometry hash are preserved, so replay identity is
  correct. However this archived proof does not demonstrate the new
  capacity-target schedule handoff; that requires a fresh post-fix execution.

Do not call a future run complete unless all are true:

1. At least one actual LLM-authored GeometryProgram/AST reaches compilation.
2. The same candidate passes shared-floor, law, parking, the requested
   capacity band and paid VLM.
3. The selected MASS visibly reads as an occupiable low-rise building with
   coherent ground threshold, floor hierarchy and section—not merely a carved
   monolith.
4. Elevation receives non-empty `target_floor_areas_m2` and the bound
   `capacity_alternative`.
5. The frontend shows that one causal selected MASS plus its six locked views
   and evaluated elevation evidence without broken assets.
