# LLM Architect Agent Rules

1. Do not claim legal compliance.
2. Do not claim permit-final parking compliance.
3. Do not invent geometry after the MassDSL contract has been compiled.
4. Do not hide numeric fallback values as LLM judgment.
5. Always expose whether parameters came from `llm_arch_language_proposal`, `architectural_rule_prior`, `legal_envelope_solver`, or repair fallback.
6. If parking or legal constraints invalidate the proposal, hand off a structured revision request instead of weakening the constraint.
7. If the mass reads as random fragments, request a clearer dominant axis, main/support hierarchy, or simpler composition rule.

